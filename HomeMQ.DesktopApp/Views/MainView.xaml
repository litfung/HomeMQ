<views:MvxWpfView x:Class="HomeMQ.DesktopApp.Views.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:HomeMQ.DesktopApp.Views"
             xmlns:views="clr-namespace:MvvmCross.Platforms.Wpf.Views;assembly=MvvmCross.Platforms.Wpf"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <DockPanel>
        <!--<StackPanel DockPanel.Dock="Left" Background="Orange">
            <Button Content="First ViewModel" Command="{Binding ShowFirstPageCommand}"/>
            <Button Content="Second ViewModel" Command="{Binding ShowSecondPageCommand}"/>
        </StackPanel>-->
        <ContentControl Content="{Binding MasterViewModel}"
                        DockPanel.Dock="Left"/>
        <ContentControl Content="{Binding DetailViewModel}"/>
    </DockPanel>
</views:MvxWpfView>
